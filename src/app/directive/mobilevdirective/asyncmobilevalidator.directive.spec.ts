import { AsyncmobilevalidatorDirective } from './asyncmobilevalidator.directive';

describe('AsyncmobilevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncmobilevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
