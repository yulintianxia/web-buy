import { MobilevalidatorDirective } from './mobilevalidator.directive';

describe('MobilevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MobilevalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
